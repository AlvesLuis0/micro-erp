<% content_for(:title, resource_title) %>

<div class="d-flex justify-content-between align-items-center">
  <h1 class="text-uppercase fs-5 fw-bold m-0"><%= resource_title %></h1>

  <%= t_link_to("helpers.submit.back", :back, class: "btn btn-secondary") %>
</div>

<hr>

<%= render("table", resources: @resources, q: @q) %>

<% content_for(:title, record_title) %>

<div class="d-flex justify-content-between align-items-center">
  <h1 class="text-uppercase fs-5 fw-bold m-0"><%= record_title %></h1>
  <%= render("components/actions", record: @record) %>
</div>

<hr>

<%= render("form", record: @record) %>